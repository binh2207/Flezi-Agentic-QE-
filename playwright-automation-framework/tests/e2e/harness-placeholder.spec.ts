import { test } from '../../fixtures';

test('harness e2e placeholder', async ({}, testInfo) => {
  testInfo.skip(
    true,
    'No e2e specs yet. Run harness-engineering (live-execution → automation-framework).',
  );
});
